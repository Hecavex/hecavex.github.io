---
const measurementToken = import.meta.env.PUBLIC_HECAVEX_ANALYTICS_TOKEN?.trim() ?? '';
---
{measurementToken && <script is:inline src="/assets/js/analytics.js" data-measurement-token={measurementToken} defer></script>}
